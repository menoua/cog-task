use super::Server;
use eframe::egui;

impl Server {
    pub(crate) fn show_cleanup(&mut self, _ctx: &egui::Context) {
        /*
        Container::new(Text::new("...").size(TEXT_XLARGE))
            .width(Length::Fill)
            .height(Length::Fill)
            .center_x()
            .center_y()
            .into()
         */
    }
}
